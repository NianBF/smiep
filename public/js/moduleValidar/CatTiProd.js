import { alertEstructura } from "../alertEstructura.js"

export const id = function(event){
    let mensaje = "completa el campo id"
    alertEstructura(event,mensaje)
}

export const nombti = function (event){
    let mensaje = "completa el campo nombre tienda"
    alertEstructura(event,mensaje)
  }

export const nombCat = function (event){
    let mensaje = "completa el campo categoria"
    alertEstructura(event,mensaje)
  }

export const empresa = function(event){
    let mensaje = "completa el campo empresa"
    alertEstructura(event,mensaje)
}
export const producto = function(event){
    let mensaje = "completa el campo producto"
    alertEstructura(event,mensaje)
}

export const estado = function (event){
    let mensaje = "selecciona un estado"
    alertEstructura(event,mensaje)
}
export const rol = function (event){
    let mensaje =  "selecciona un rol"
    alertEstructura(event,mensaje)
}

export const selectTi = function (event){
    let mensaje =  "seleccione una tienda"
    alertEstructura(event,mensaje)
}

export const imagen = function(event){
    let mensaje = "completa el campo imagen"
    alertEstructura(event,mensaje)
}

export const precio = function(event){
    let mensaje = "completa el campo precio"
    alertEstructura(event,mensaje)
}

export  const cantDisp = function(event){
    let mensaje = "completa el campo cantidad disponible"
    alertEstructura(event,mensaje)
}

export const presentacion = function(event){
    let mensaje = "completa el campo tipo de presentacion"
    alertEstructura(event,mensaje)
}

export const descrip = function(event){
    let mensaje ="completa el campo descripcion"
    alertEstructura(event,mensaje)
}

export const codBar = function(event){
    let mensaje = "completa el campo codigo de barras"
    alertEstructura(event,mensaje)
}

export const priceArrive = function(event){
    let mensaje = "complete el campo precio de llegada"
    alertEstructura(event,mensaje)
}